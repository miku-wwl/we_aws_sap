这 10 道题目涵盖了 **EC2 实例限制 (SCP vs IAM vs Tagging)**、**S3 复制监控 (S3 RTC)**、**迁移规划工具 (Discovery / Migration Hub)**、**Multi-AZ 高可用改造**、**容器化迁移 (Fargate + EFS)**、**CloudTrail 组织级审计**、**HPC/集群低延迟网络 (Cluster Placement Group)**、**大规模本地文件备份 (DataSync)**、**EMR 多角色访问控制 (Service Catalog)**。

特别是 **Q352 (S3 RTC)** 和 **Q360 (Service Catalog for EMR)** 是 SAP 考试中非常细节的考点。

让我们开启 **逐题秒杀模式**！

---

#### 📝 [351/529] 限制 EC2 实例类型与区域 (SCP)

**1. 🕵️‍♂️ 题眼与约束分析**
* **需求：**
    1.  Only allow `t3.small`.
    2.  Only allow `us-east-2`.
    3.  **Account CANNOT be part of AWS Organizations** (账户不能加入 Organizations，为了保持在企业 IT 之外)。
* **这是一个陷阱！** 题目明确说 "Project's account CANNOT be part of... Organizations"。
    * 这意味着 **SCP (Service Control Policy)** 不可用！SCP 必须依赖 Organizations。
    * $\rightarrow$ **排除 B** (Create an SCP)。
* **选项分析：**
    * **A:** "Add account to company's Organizations". 违反题目 "Cannot be part of..."。排除。
    * **C:** 购买预留实例？购买 RI 不会限制用户启动其他类型的实例（只是其他类型按 On-Demand 计费）。不能强制合规。排除。
    * **D:** 创建 **IAM Policy**。
        * IAM Policy 可以包含 `Condition` 元素。
        * `ec2:InstanceType` == `t3.small`。
        * `aws:RequestedRegion` == `us-east-2`。
        * 将策略附加给开发人员的角色/组。
        * 这在单账户（非 Org）环境下是实现强制控制的标准方法。
    * **锁定 D。**

**3. ✅ 正确选项解析 (选项 D)**
* **IAM Policy Condition:** 在单账户环境下，通过 `ec2:InstanceType` 和 `aws:RequestedRegion` 条件键强制合规。

**5. 📚 核心考点:** 单账户环境下的资源限制 (IAM Policy) vs 多账户 (SCP)。

---

#### 📝 [352/529] S3 复制监控与 SLA (S3 RTC)

**1. 🕵️‍♂️ 题眼与约束分析**
* **场景：** 跨账户 S3 复制 (CRR)。
* **需求：** 监控特定雷达站（最准确数据）的复制，必须在 **15 minutes** (题目说 30 分钟? 选项通常对应 **S3 RTC** 的 15 分钟 SLA) 内完成。
* **关键指标：** 监控复制完成时间。

**2. ⚡ 秒杀思路**
* **S3 原生特性：**
    * **S3 Replication Time Control (S3 RTC):** AWS 承诺在 **15 分钟** 内复制 99.99% 的对象。
    * RTC 提供了专门的 CloudWatch 指标，如 `ReplicationLatency`。
    * 并且支持在复制延迟超过阈值时触发 EventBridge (S3 Replication metrics)。
* **选项对比：**
    * **A (DataSync):** DataSync 是搬运数据的，不是监控 S3 原生 CRR 的。S3 CRR 是异步的后台过程。
    * **B (Separate Bucket):** 将特定数据分到新桶？虽然可以隔离，但还要自己“监控最大复制时间”？S3 标准复制不提供 SLA，也没有原生的“复制耗时”事件（除非开启 RTC）。
    * **C (Transfer Acceleration):** 这是加速**上传**的，跟**复制**（Replication）速度无关。复制是 AWS 后端完成的。
    * **D:**
        * 创建 Replication Rule 过滤特定前缀（最准确雷达站）。
        * Enable **S3 RTC** (Replication Time Control)。
        * 监控并报警。
        * 这是利用 S3 原生 SLA 特性的标准解法。
    * **锁定 D。**

**3. ✅ 正确选项解析 (选项 D)**
* **S3 RTC:** 提供 15 分钟复制 SLA 和详细的监控指标。

**5. 📚 核心考点:** S3 跨区域复制的 SLA 监控 (RTC)。

---

#### 📝 [353/529] 迁移评估与成本估算工具 (Discovery + Migration Evaluator)

**1. 🕵️‍♂️ 题眼与约束分析**
* **环境：** Thousands of VMs, SAN, Databases, Dependencies.
* **痛点：** Documentation incomplete (文档缺失)。
* **任务：**
    1.  Understand current environment (了解现状/依赖)。
    2.  Estimate cloud costs (估算成本)。
* **选择：** 3 个工具/服务。

**2. ⚡ 秒杀思路**
* **了解现状/依赖：**
    * **AWS Application Discovery Service (ADS) (A):** 收集服务器配置、使用情况和**网络依赖**。适合“文档缺失”的场景。 $\rightarrow$ **选中 A**。
    * **Migration Hub (F):** 作为迁移的**中央仪表盘**，它集成了 ADS 的数据，可以可视化服务器和应用分组。 $\rightarrow$ **选中 F**。
* **估算成本：**
    * **AWS Cloud Adoption Readiness Tool (CART) (D):** 这是一个问卷调查，评估组织的云就绪程度（人员、流程等），不直接做技术发现或成本估算。
    * **Migration Evaluator (以前的 TSO Logic):** 专门做 TCO (总拥有成本) 分析。但选项里没有 Migration Evaluator？
    * 让我们再看一遍选项：
        * A: ADS (发现)。
        * B: SMS (迁移工具，旧)。
        * C: X-Ray (应用链路追踪，需要改代码)。
        * D: CART (就绪评估)。
        * E: Inspector (安全评估)。
        * F: Migration Hub (管理中心)。
    * **这里有个问题：** 真正做“成本估算”的工具是 Migration Evaluator (TSO Logic)。如果选项里没有，通常 Migration Hub 配合 ADS 数据可以导出到 Athena 或 QuickSight 做分析，或者 Migration Hub Strategy Recommendations。
    * **CART (D)** 虽然是评估工具，但主要产出是“就绪报告”。
    * **然而，** 在给出的选项中，**A (ADS)** 和 **F (Migration Hub)** 是必选的（发现+管理）。第三个选谁？
    * **B (SMS)** 是迁移执行。
    * **C (X-Ray)** 不适用。
    * **E (Inspector)** 是安全。
    * **D (CART)** 是迁移规划早期常用的工具，虽然它不直接扫描服务器算钱，但它属于“Plan cloud migration”这一大类活动的一部分。
    * **但是，** 如果有 **Migration Evaluator** (以前叫 TSO Logic)，那绝对是首选。
    * **让我们假设：** 题目可能是旧题，或者选项缺失。在没有 Evaluator 的情况下，通常 **A, D, F** 是规划阶段的组合。或者 **A, B, F** (发现+迁移+管理)？
    * 题目问 "Plan cloud migration... understand environment... estimate cost"。
    * **ADS (A)** 收集数据。
    * **Migration Hub (F)** 聚合数据。
    * 谁估算成本？ADS 的数据可以导出用来估算。
    * **CART (D)** 生成迁移计划报告。
    * 相比之下，SMS 是执行。
    * **结论：** A, D, F 是最贴切“规划”阶段的。
    * **修正：** 有些版本可能有 Migration Evaluator 选项。如果这里必须选，A, F 核心。D 作为辅助。

**3. ✅ 正确选项解析 (选项 A, D, F)**
* **ADS:** 发现服务器和依赖。
* **Migration Hub:** 跟踪进度和分组。
* **CART:** 评估组织就绪度（规划的一部分）。

**5. 📚 核心考点:** 迁移规划阶段的工具链。

---

#### 📝 [354/529] 单 AZ 架构改造为 Multi-AZ (NAT GW + Multi-AZ RDS)

**1. 🕵️‍♂️ 题眼与约束分析**
* **现状：** EC2 in ASG (1 Min/Max), Single AZ. RDS MySQL Single AZ. NAT Gateway (Single AZ).
* **目标：** Operate across **Multiple AZs** (多可用区高可用)。

**2. ⚡ 秒杀思路**
* **NAT Gateway:**
    * NAT GW 是 AZ 级别的资源。要实现多 AZ 高可用，必须在**每个 AZ 部署一个 NAT GW**。
    * $\rightarrow$ **选中 A 或 D**。
* **数据库:**
    * 将 RDS 修改为 **Multi-AZ** 部署。这是 RDS 高可用的标准开关。
    * $\rightarrow$ **选中 A**。
    * (D 选项说 "Enable automated backups"? 备份不是 HA。Multi-AZ 才是 HA)。
* **Auto Scaling:**
    * 要跨 AZ 运行，ASG 需要配置使用多个子网（跨 AZ）。
    * 容量：为了高可用，通常建议 Min/Max > 1。A 选项建议设为 3（3个AZ各一个？），这提高了可用性。D 选项保持为 1，这意味着虽然跨 AZ 配置了，但同时只有一个实例跑，如果那个 AZ 挂了，ASG 会在另一个 AZ 重启。这也是一种 HA（Self-healing），但 A 的主动多活更好。
    * **决胜点：** D 选项的 RDS 没开 Multi-AZ（只开了备份）。这不满足 "Operate across multiple AZs" 对数据库层面的要求（单 AZ 数据库挂了就停机了）。A 选项明确开了 Multi-AZ RDS。
    * **锁定 A。**

**3. ✅ 正确选项解析 (选项 A)**
* **Multi-AZ NAT:** 消除网络单点。
* **Multi-AZ RDS:** 消除数据库单点。
* **ASG Spanning AZs:** 消除计算单点。

**5. 📚 核心考点:** 标准 VPC 三层架构的高可用改造步骤。

---

#### 📝 [355/529] 容器化事务应用迁移 (Fargate + EFS)

**1. 🕵️‍♂️ 题眼与约束分析**
* **源：** Docker on VM。
* **存储：** Shared storage for transaction data (共享存储)。
* **需求：**
    1.  Low latency storage (低延迟存储)。
    2.  **Auto-scale throughput** (自动扩展吞吐)。
    3.  **No further development** (不改代码 -> 还是写文件)。
    4.  **No manage Docker env** (不管理环境 -> Serverless)。
* **迁移：** To AWS。

**2. ⚡ 秒杀思路**
* **计算平台：**
    * "No manage Docker hosting environment" $\rightarrow$ **AWS Fargate** (ECS 或 EKS)。
    * 选项 B/C 是 Fargate。A 是 EKS（需要管集群？EKS Fargate 也可以，但通常 ECS Fargate 更简单）。D 是 EC2（需要管）。
* **存储选型：**
    * 必须是 **Shared Storage** (共享存储)。
    * **EFS (B):** 是 AWS 的共享文件系统，原生支持 ECS Fargate 挂载。EFS 支持 **Elastic Throughput** (自动扩展吞吐) 和低延迟访问。
    * **EBS (C):** EBS 主要是单机块存储。虽然有多重挂载 (Multi-Attach)，但不支持 Fargate，且主要用于特定集群文件系统。对于通用 Docker 共享，EFS 是标准。
    * **S3 (A):** 对象存储，不是文件系统。应用原本是写文件的，改 S3 API 违反 "No further development"。
* **结论：** **Fargate + EFS** 是运行需要共享文件系统的容器化应用的最佳 Serverless 组合。
* **锁定 B。**

**3. ✅ 正确选项解析 (选项 B)**
* **Fargate:** Serverless 容器计算。
* **EFS:** Serverless 共享文件存储，支持吞吐自动扩展。

**5. 📚 核心考点:** 有状态容器应用（共享文件）的 Serverless 迁移架构。

---

#### 📝 [356/529] 迁移前发现与规划 (Discovery Agent + Migration Hub)

**1. 🕵️‍♂️ 题眼与约束分析**
* **现状：** Physical/Virtual mix, No accurate inventory (无清单)。
* **需求：**
    1.  Info about network connections & dependencies (网络依赖)。
    2.  Right-size resources (资源调整)。
    3.  Develop migration plan (制定计划)。
* **目标：** Solution to provide info。

**2. ⚡ 秒杀思路**
* **工具选型：**
    * **AWS Application Discovery Service (ADS):**
        * **Agentless Collector (VMware):** 收集 VM 配置，但**不收集详细网络依赖**（只能看到基本的）。
        * **Discovery Agent (Agent-based):** 安装在 OS 内部，能收集详细的**进程级网络连接**和**性能数据**。题目要求 "Network connections and application relationships" 且要 "Right-size"（需要详细性能数据），Agent 是必须的。
    * **Migration Hub:** 集中展示数据。
    * **Strategy Recommendations:** 根据收集的数据推荐迁移策略（Rehost/Replatform等）。
* **选项对比：**
    * A (Evaluator + Agentless): Agentless 看不到详细网络依赖。
    * **B:** Install **ADS Agent**... Use **Strategy Recommendations** collector (这可能是指 Strategy Recommendations 的功能，虽然术语有点混，但由 Agent 收集数据是关键)。
    * C (Agentless): 同 A，看不到详细依赖。
    * D (Import tool): 导入什么？没清单啊。
    * **关键判据：** **"Info about network connections"**。只有 **Agent-based** Discovery Service 能提供高精度的网络依赖图。
    * **锁定 B。**

**3. ✅ 正确选项解析 (选项 B)**
* **Discovery Agent:** 能够捕获进程级网络通信，用于依赖分析。
* **Migration Hub Strategy Recommendations:** 自动化生成迁移路径建议。

**5. 📚 核心考点:** Discovery Service Agent vs Agentless 的能力区别（网络依赖）。

---

#### 📝 [357/529] 组织级 CloudTrail 审计 (Org Trail)

**1. 🕵️‍♂️ 题眼与约束分析**
* **场景：** SaaS Platform, Multi-account (Organizations)。
* **需求：** Audit ALL API calls (审计所有调用)，Track changes, Durable & Secure storage。
* **目标：** **Least operational overhead**。

**2. ⚡ 秒杀思路**
* **Organization Trail:**
    * 在 **Management Account** 创建一个 **Organization Trail**。
    * 它会自动应用到组织内的**所有**现有和未来账户。
    * 日志统一投递到 Management Account 的 S3 桶。
    * 这样无需在每个成员账户单独配置 (B 错)。
* **安全性：**
    * 启用 S3 MFA Delete 和 Encryption (A/C/D 都提到了)。
    * Log File Validation (防篡改)。
* **选项对比：**
    * A: "Create a new CloudTrail trail... deploy to all regions". 没明确说是 "Organization Trail"。如果是普通 Trail，它只管当前账户。Organization Trail 是一个特定选项 "Enable for all accounts in my organization"。
    * C: "Create a new CloudTrail trail in Management Account... **Deploy the trail for all accounts in the organization**". 这就是 Organization Trail 的定义。
    * D: 发 SNS 到外部系统？增加 Overhead。
    * **A vs C:** C 的描述 "Deploy... for all accounts" 更准确地对应了 Organization Trail 的功能开关。A 可能被解读为只在管理账户开 Trail。
    * **版本控制：** C 提到了 "New S3 bucket with **Versioning**"。这是数据持久性和防篡改的重要一环。
    * **锁定 C。**

**3. ✅ 正确选项解析 (选项 C)**
* **Organization Trail:** 一键开启全组织审计。
* **S3 Versioning & MFA Delete:** 确保日志数据的不可变性与持久性。

**5. 📚 核心考点:** AWS Organizations 环境下的集中式 CloudTrail 配置。

---

#### 📝 [358/529] 分布式内存数据库低延迟网络 (Cluster PG + Memory Optimized)

**1. 🕵️‍♂️ 题眼与约束分析**
* **应用：** Distributed in-memory database (分布式内存数据库)。
* **架构：** 1 Master + 8 Workers。
* **需求：** **Lowest possible network latency** (最低延迟) for max performance。

**2. ⚡ 秒杀思路**
* **实例类型：**
    * 内存数据库 $\rightarrow$ **Memory Optimized** (R/X 系列)。
    * $\rightarrow$ 排除 B/D (Compute Optimized)。
* **放置组 (Placement Group):**
    * **Cluster (集群):** 将实例物理上紧密放置（同一机架或相邻），提供最低延迟和最高单流带宽。适合 HPC 和紧密耦合的分布式数据库。
    * **Partition (分区):** 隔离故障，用于 Hadoop/Kafka。延迟不如 Cluster。
    * **Spread (分散):** 物理隔离，防单点，延迟最高。
    * $\rightarrow$ **选中 C** (Cluster PG)。
* **锁定 C。**

**3. ✅ 正确选项解析 (选项 C)**
* **Memory Optimized Instances:** 适配内存数据库。
* **Cluster Placement Group:** 适配低延迟通信需求。

**5. 📚 核心考点:** 高性能计算/数据库的网络优化配置。

---

#### 📝 [359/529] 100万小文件增量备份 (DataSync)

**1. 🕵️‍♂️ 题眼与约束分析**
* **源：** 1 Million .csv files (10TB + 1TB/week growth)。
* **需求：**
    1.  Automate backup to AWS (自动化)。
    2.  Daily (每日)。
    3.  **Custom filter** (只备份子集)。
* **连接：** DX。
* **目标：** **Least operational overhead**。

**2. ⚡ 秒杀思路**
* **工具选型：**
    * **S3 CopyObject (A):** S3 API 是云端操作，不能直接把本地文件 Copy 上去（需要 `PutObject`）。而且对于 100 万个文件，自己写脚本管理增量、重试、并发非常麻烦。
    * **AWS Backup (B):** AWS Backup 最近支持了 S3，但它主要是管理 AWS 内部资源的。对于本地文件，AWS Backup 通常通过 Storage Gateway 或 DataSync 集成。单独说 "Create backup plan" 没说明怎么传数据。
    * **Snowball (D):** 每日 1TB 增量？每天寄一个快递？不现实。
    * **AWS DataSync (C):**
        * 专为传输大量文件设计。
        * 支持 **Filters** (Include/Exclude patterns)，满足 "subset of data" 需求。
        * 支持 **Schedule** (Daily)。
        * 支持 **Incremental** (只传增量)。
        * 性能极高（并行传输）。
    * $\rightarrow$ **选中 C**。
* **锁定 C。**

**3. ✅ 正确选项解析 (选项 C)**
* **DataSync:** 处理海量小文件传输、增量同步和过滤规则的最佳托管服务。

**5. 📚 核心考点:** 大规模本地文件上云的传输工具选型。

---

#### 📝 [360/529] EMR 多角色权限管理 (Service Catalog)

**1. 🕵️‍♂️ 题眼与约束分析**
* **场景：** EMR 分析平台。
* **角色：** Admin, Data Engineer, Data Analyst (不同权限)。
* **需求：**
    1.  Least privilege (最小权限)。
    2.  Launch only **approved/authorized** apps (仅启动批准的应用)。
    3.  **Ensure tagging** (强制标签)。
* **目标：** 满足要求。

**2. ⚡ 秒杀思路**
* **Service Catalog (C):**
    * **预定义产品：** 可以为不同角色定义不同的 EMR 集群模板（Product）。
    * **权限控制：** 用户只需要有启动 Service Catalog Product 的权限，不需要底层的 `emr:RunJobFlow` 权限（通过 Launch Role 代理）。这完美实现了 "Launch only approved apps"。
    * **强制标签：** Service Catalog 支持 **TagOptions**，可以强制用户在启动时选择或自动应用标签。
    * **多角色：** 可以将不同的 Product 组合（Portfolio）共享给不同的 IAM 角色（Admin, Engineer, Analyst）。
    * $\rightarrow$ **选中 C**。
* **选项对比：**
    * A (Config): Config 是事后审计。
    * B (Kerberos): 主要是做 EMR 内部的用户认证（Hadoop 用户），不解决 AWS 资源（集群本身）的创建和标签控制。
    * D (CloudFormation + Config): 虽然也能做，但不如 Service Catalog 在“自助服务”和“权限隔离”方面封装得好。Service Catalog 是治理层。
* **锁定 C。**

**3. ✅ 正确选项解析 (选项 C)**
* **AWS Service Catalog:** 实现自助式资源调配、强制合规配置（如标签）和细粒度访问控制。

**5. 📚 核心考点:** Service Catalog 在企业级资源治理中的应用。

---
**小结：**
这组题目的 **DataSync 过滤**、**Cluster PG**、**Organization CloudTrail** 都是实战高频。

**恭喜你，360 题！**