#### 📝 [80/529] IoT 平台高并发优化

**1. 🕵️‍♂️ 题眼与约束分析**
* **瓶颈：** API Server 过载，RDS 写入延迟高。
* **目标：** 应对海量增长，成本高效。

**2. ⚡ 秒杀思路**
* **解耦：** API Server 同步写库太脆弱 $\rightarrow$ 引入 **Kinesis** 缓冲。
* **换库：** RDS MySQL 抗不住海量 IoT 写入 $\rightarrow$ 换 **DynamoDB**。
* **锁定 C, E。**

**3. ✅ 正确选项解析 (选项 C, E)**
* **C (Kinesis + Lambda):** Kinesis 可以吸收瞬间的流量洪峰（削峰填谷），Lambda 异步消费，保护后端。
* **E (DynamoDB):** 对于 IoT 这种"高并发、简单结构"的数据，DynamoDB 的写入扩展性远超 RDS，且由 Serverless 自动扩容。

**4. ❌ 错误选项排查**
* **选项 A (EBS Upscale):**
    * *错误原因：* **治标不治本**。增加 IOPS 只是暂缓，单机数据库连接数和 CPU 迟早还是瓶颈。
* **选项 B (Aurora):**
    * *错误原因：* 虽然 Aurora 比 RDS 强，但如果是纯写入负载，DynamoDB 性价比更高。且 B 没有解决 API Server 的前端压力问题。
* **选项 D (X-Ray):**
    * *错误原因：* **监控不是优化**。增加 API Server 也解决不了数据库的瓶颈。

**5. 📚 核心考点:** IoT 高并发写入架构 (Kinesis -> Lambda -> DynamoDB)。