#### 📝 [407/529] EC2 使用量激增报警 (AWS Budgets)

**1. 🕵️‍♂️ 题眼与约束分析**
* **需求：** Track EC2 usage (hours/cost)。
* **报警条件：** Usage > 10% higher than **average of past 30 days** (比过去30天平均值高10%)。
* **工具：** Organizations。

**2. ⚡ 秒杀思路**
* **AWS Budgets (A):**
    * 支持 **Auto-adjusting budgets** (自动调整预算)。可以基于历史期间（如过去 30 天或上个月）的平均值或支出设置基线。
    * 题目描述 "Set budget amount to 10% higher than average usage... reported by Cost Explorer"。这正是 AWS Budgets 的功能（可以基于 Cost Explorer 数据手动设，或者用 Auto-adjusting）。
    * 选项 A 的描述是手动的？"Set budget amount to..."。如果能获取到过去30天平均值，这确实能做到。
* **Cost Anomaly Detection (B):**
    * 专门检测异常（Anomaly）。它使用 ML 自动检测峰值。
    * 题目有一个非常具体的阈值 "10% higher than average"。Cost Anomaly Detection 通常是检测"异常"，阈值是基于影响金额（Impact）或敏感度，而不是简单的"比平均高10%"这种固定逻辑（虽然效果类似，但机制不同）。
    * **Budgets** 更适合这种"基于规则的固定阈值"报警。
    * **但是，** A 选项说 "Specify a daily period... Set amount to 10% higher than past 30 days"。如果每天都要手动设，那太累了。
    * **让我们再看 B:** Cost Anomaly Detection 是自动的。如果 "10% higher" 是指"显著异常"，B 更智能。
    * **然而，** 题目中的 "10% higher than average of past 30 days" 是一个非常具体的**预算逻辑**。AWS Budgets 的 **Auto-adjusting budgets** 功能可以配置为 "Based on historical usage"，并设置 Notification threshold。
    * 让我们仔细看 A 的操作性。如果是"一次性配置"一个 Auto-adjusting Budget，那是完美的。如果是 A 描述的"手动填一个数字"，那这就不是动态的。但 A 的描述更像是"配置逻辑"。
    * 考虑到 **Cost Anomaly Detection** 主要是为了发现意料之外的峰值（ML based），而题目定义了明确的数学规则，**AWS Budgets** 是执行这种规则的标准工具。
    * **锁定 A。**

**3. ✅ 正确选项解析 (选项 A)**
* **AWS Budgets:** 支持基于使用量（Usage）的预算和报警。

**5. 📚 核心考点:** 成本监控工具的区别 (Budgets vs Anomaly Detection)。

---
**恭喜你，407 题！**