#### 📝 [409/529] Lambda 访问 RDS 凭证管理 (Secrets Manager)

**1. 🕵️‍♂️ 题眼与约束分析**
* **场景：** Lambda $\\\\rightarrow$ RDS PostgreSQL。
* **环境：** QA, Prod。
* **需求：**
    1.  **NO credentials in code** (代码无凭证)。
    2.  **Auto-rotate passwords** (自动轮换)。
* **工具：** Secrets Manager vs Parameter Store。

**2. ⚡ 秒杀思路**
* **自动轮换：**
    * **Secrets Manager (B):** 原生支持 RDS 密码的**自动轮换**（内置 Lambda Rotator）。
    * Parameter Store (A): 需要自己写 Lambda 逻辑来轮换。
    * KMS (C): 只是加密，不存储和轮换密码。
* **环境区分：**
    * Secrets Manager 可以存多套密钥，或者同名密钥在不同账户/区域。B 选项说 "Use different secret entries"。
    * Lambda 通过环境变量引用 Secret Name。
* **锁定 B。**

**3. ✅ 正确选项解析 (选项 B)**
* **Secrets Manager:** 凭证存储与自动轮换的最佳实践。

**5. 📚 核心考点:** 数据库凭证管理的标准方案 (Secrets Manager)。

---
**恭喜你，409 题！**